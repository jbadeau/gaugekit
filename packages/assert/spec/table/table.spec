# Assert - Table

* expect that <table> is empty
* expect that <table> is not empty

* expect that <table> has row count <count>
* expect that <table> has not row count <count>

* expect that <table> has column count <count>
* expect that <table> has not column count<count>

* expect that <table> has column name <name>
* expect that <table> has not column name<name>

* expect that <table> has column names <names>
* expect that <table> has not column names <names>