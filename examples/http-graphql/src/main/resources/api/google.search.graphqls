# API Name: "Google Search"
# Host: http://localhost:8080/api

type Resource {
    id: Int!
    title: String!
    link: String!
    description: String!
}

type Query {
    search(query: String!): [Resource!]
}

schema {
    query: Query
}
